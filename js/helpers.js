window.ArchiveHelpers=(()=>{"use strict";function escapeHTML(value=""){return String(value).replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[ch]))}function formatDate(dateString,locale="en-GB",options={day:"2-digit",month:"long",year:"numeric"}){const d=new Date(`${dateString}T12:00:00`);return new Intl.DateTimeFormat(locale,options).format(d)}function groupByDate(messages){return messages.reduce((acc,msg)=>{(acc[msg.date]??=[]).push(msg);return acc},{})}function sortMessages(messages){return [...messages].sort((a,b)=>`${b.date}T${b.time}`.localeCompare(`${a.date}T${a.time}`))}function monthKey(date){return date.slice(0,7)}return{escapeHTML,formatDate,groupByDate,sortMessages,monthKey}})();
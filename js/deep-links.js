(()=>{"use strict";const toast=document.querySelector("#toast");let timer;function showToast(message){if(!toast)return;toast.textContent=message;toast.classList.add("is-visible");clearTimeout(timer);timer=setTimeout(()=>toast.classList.remove("is-visible"),1800)}async function copyMessageLink(id){const url=new URL(location.href);url.hash=id;try{await navigator.clipboard.writeText(url.toString());showToast("Message link copied")}catch{prompt("Copy this link:",url.toString())}}function bindCopyLinks(){document.querySelectorAll("[data-copy-link]").forEach(button=>{if(button.dataset.bound)return;button.dataset.bound="1";button.addEventListener("click",()=>{const id=button.getAttribute("data-copy-link");if(id)copyMessageLink(id)})})}function reveal(){const id=location.hash.slice(1);if(!id)return;const target=document.getElementById(id);if(target)setTimeout(()=>target.scrollIntoView({behavior:"smooth",block:"center"}),80)}document.addEventListener("archive:rendered",()=>{bindCopyLinks();document.querySelectorAll('.message-media img').forEach(img=>{if(img.complete)img.classList.add("loaded");else img.addEventListener("load",()=>img.classList.add("loaded"),{once:true})});reveal()});addEventListener("hashchange",reveal)})();
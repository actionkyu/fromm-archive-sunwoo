(()=>{"use strict";async function init(){try{const response=await fetch("data/messages.json",{cache:"no-store"});if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json();window.ArchiveRenderer.render(data)}catch(error){const root=document.querySelector("#archiveRoot");if(root)root.innerHTML=`<p class="error-state">Could not load <code>data/messages.json</code>. When testing locally, start a small web server instead of opening the file directly.<br><br>${String(error.message||error)}</p>`;console.error(error)}}window.addEventListener("DOMContentLoaded",init)})();